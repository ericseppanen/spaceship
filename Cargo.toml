[package]
name = "spaceship"
version = "0.1.0"
edition = "2021"

[profile.dev]
opt-level = 1

[profile.dev.package."*"]
opt-level = 3

[dependencies]
bevy = { version = "0.12.1", features = ["wav"] }
fastrand = "2.0.1"
winit = { version = "0.28.7", default-features = false }
